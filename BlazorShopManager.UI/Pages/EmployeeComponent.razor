@page "/employeecomponent"
<PageTitle>@Title</PageTitle>
<h2>Employee</h2>

@if (Employees is null)
{
    <p><em>Loading...</em></p>
}
else
{
    <QuickViewPopupComponent EmployeeQuickView="_selectedEmployee"></QuickViewPopupComponent>

    <div class="row">
        @*@foreach(var item in Employees)
        {
            <div class="col-12 col-sm-12 col-md-6 col-lg-4 col-xl-3">
                <EmployeeCardComponent EmployeeCard="item" EmployeeQuickViewClicked="ShowQuickViewPopup"></EmployeeCardComponent>
            </div>
        }*@

        @foreach (var item in Employees)
        {
            <div class="col-12 col-sm-12 col-md-6 col-lg-4 col-xl-3">
                <ErrorBoundary>
                    <ChildContent>
                        <EmployeeCardComponent EmployeeCard="item" EmployeeQuickViewClicked="ShowQuickViewPopup"></EmployeeCardComponent>
                    </ChildContent>
                    <ErrorContent>
                        <p class="errorUI">Invalid Employee!</p>
                    </ErrorContent>
                </ErrorBoundary>                
            </div>
        }
    </div>


    @*<table class="table table-light">
        <thead class="table-primary">
            <tr>
                <th></th>
                <th>Id</th>
                <th>First Name</th>
                <th>Last Name</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Employees)
            {
                <tr>
                    <td><img src="@($"https://gillcleerenpluralsight.blob.core.windows.net/person/{item.EmployeeId}-small.jpg")" /></td>
                    <td>@item.EmployeeId</td>
                    <td>@item.FirstName</td>
                    <td>@item.LastName</td>
                </tr>
            }
        </tbody>
    </table>*@
}